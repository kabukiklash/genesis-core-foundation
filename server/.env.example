# GenesisCore Runtime Configuration

# Server port
PORT=3000

# CORS origin (use * for development, restrict in production)
CORS_ORIGIN=*

# Database path (relative to server directory)
GENESIS_DB_PATH=./genesis.db

# Environment
NODE_ENV=development
